// ═══════════════════════════════════════════
// spotify.js — Spotify Web API integration
// 
// FUTURE: Link songs in the Song Library to Spotify tracks.
// Uses Spotify Web Playback SDK and OAuth PKCE flow.
// Credentials: stored in localStorage (never in this file).
//
// Planned functions:
//   initSpotify()        — handle OAuth redirect, store token
//   searchTrack(title)   — returns { id, uri, preview_url }
//   linkSongToSpotify(songId, spotifyUri)
//   getEmbedUrl(spotifyUri) — returns iframe embed URL
// ═══════════════════════════════════════════

// Placeholder — not yet implemented
