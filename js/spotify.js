// ═══════════════════════════════════════════
// spotify.js — Spotify Web API integration (future)
//
// Planned functions:
//   initSpotify()        — handle OAuth redirect, store token
//   searchTrack(title)   — returns { id, uri, preview_url }
//   linkSongToSpotify(songId, spotifyUri)
//   getEmbedUrl(spotifyUri) — returns iframe embed URL
// ═══════════════════════════════════════════

// Placeholder — not yet implemented
